#!/usr/bin/env bash
# Eezifin MCP Server launcher.
# Spawned by Claude Code via MCP settings — communicates over STDIO.
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PROJECT_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"

exec npx tsx "$PROJECT_ROOT/src/mcp/eezifin/index.ts"
